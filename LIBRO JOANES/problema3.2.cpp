#include <cstdlib> // PARA QUE UTILIZAMOS ESTA LIBRERIA
#include <iostream>
using namespace std;
int main(int argc , char * argv[]) // QUE SIGNIFICAN ESOS PARAMETROS
{
    int x;
    cout<< "dame un numero entero";
    cin >> x; x = 2 * x;
    cout << "su doble es"<< x <<"su triple es" << 3 * x << endl;
    system("pause");
    
    return 0;
}
#include <cstdlib> // PARA QUE UTILIZAMOS ESTA LIBRERIA
#include<cstdlb> para indicar al copilador que inserte el codigo fuente en el registro programa y lo compile con el programa principal
<cstdlib> se utiliza para convertir un entero a cadena de caracteres

int main(int argc , char * argv[]) // QUE SIGNIFICAN ESOS PARAMETROS
intes el tipo de dato de retorno de la función
main():Indica que la función devuelve
int argc: es el nurmero de argumetros que se pasan a su progrma desde la linea de comandos.
    char:se usa para almacenar el valor entero 
    argv: es la direccion de una tabla

